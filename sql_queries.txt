data_base name :- betting

CREATE TABLE CricketMatches (
    match_id INT PRIMARY KEY,
    team_one VARCHAR(255),
    team_two VARCHAR(255),
    team_one_score VARCHAR(255),
    team_two_score VARCHAR(255),
    url VARCHAR(255),
    is_live_match BOOLEAN
);


